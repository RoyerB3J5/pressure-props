---
import Button from '../../components/Button.astro';

type Translations = typeof import('../../i18n/es').default;
type CompletedContent = Translations['main']['completed'];
interface Props {
  content: CompletedContent;
}
const { content } = Astro.props as Props;
---
<section class="flex flex-col justify-center items-center gap-[115px] py-[115px] overflow-y-visible ">
  <div class="flex flex-col justify-center items-center gap-[53px] max-w-[1215px] w-full">
    <div class="flex justify-between items-center px-8 w-full">
      <h2 set:html={content.title} class="text-primary-dark-bold text-[40px] font-normal leading-[46px] text-start"></h2>
      <Button class='button-gradient px-[38px]'>
          {content.button}
        </Button>
    </div>
  </div>
  <div class="flex flex-col justify-center items-center gap-7 w-full max-w-[1138]">
    <div class="w-full flex justify-center items-center gap-[26px] px-3">
      <img src="/main/logo.webp" alt="Logo de PressureProps" width="1024" height="1024" class="w-[62px] h-[62px] rounded-full border-[2px] border-primary-dark-bold"/>
      <div class="flex justify-start items-center gap-[3px]">
        <h3 class="text-[20px] font-bold leading-[28px] text-start text-primary-dark-bold">{content.name}</h3>
        <p class="text-[16px] font-normal leading-[19.6px] text-start text-primary-light">{content.description}</p>
      </div>
    </div>
    <div class="grid grid-cols sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-[10px] w-full -mb-56">
      {Array.from({ length: 16 }).map((_, index) => (
        <div class="w-[277px] h-[277px] bg-[#D9D9D9] flex justify-center items-center">
        </div>
      ))}
    </div>
  </div>
</section>